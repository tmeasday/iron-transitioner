1. override router to add extra functions
  // Router.prototype.back
  // Router.prototype.setNextTransitionType
  // Router.prototype.transitionType

2. Create Transitioned Page manager that provides extra helper
  transitionedYield -- which calls _renderTransitionedTemplate
  
3. _renderTransitionedTemplate:

ii. renders a Templates._transitionedTemplate with 

 data = _.extend(data, {_transitionManager: TM});

3. Templates._transitionedTemplate's rendered callback:

  - checks if it's in the DOM yet. If so:
  
    renders currentTemplate into firstPane.
    
  - else does it when it gets put into the dom

  - sets up a callback on this.data._transitionManager.setTemplate(new, type)